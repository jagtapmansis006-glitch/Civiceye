export function SiteFooter() {
  return (
    <footer className="relative z-10 border-t border-black/5">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-6 py-6 text-sm text-ink/55 sm:flex-row sm:items-center sm:justify-between">
        <p>CivicEye · Municipal works division</p>
        <p>Every report is a public record. Response standards are published.</p>
      </div>
    </footer>
  );
}
